fx Pen {
    pass Pass0 {
        state.depth.func = GL_ALWAYS;

        vs = "#include <Shaders\edge_line_billboard_gs.vert>";
        gs = "#include <Shaders\pen.geom>";
        fs = "#include <Shaders\pen.frag>";
    }
}
