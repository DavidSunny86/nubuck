fx SDText {
    pass Pass0 {
        state.alphaTest {
            enabled = GL_TRUE;
            func    = GL_GEQUAL;
            ref     = 0.5;
        }

        vs = "#include <Shaders\text.vert>";
        fs = "#include <Shaders\text.frag>";
    }
}
